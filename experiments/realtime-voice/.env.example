# Required — stays server-side, never reaches the browser.
OPENAI_API_KEY=sk-...

# Optional — wires the handoff to a real Claude call instead of the stub.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides. Check current model names against provider docs.
# REALTIME_MODEL=gpt-realtime
# REALTIME_VOICE=cedar
# DEEP_MODEL=claude-sonnet-5
